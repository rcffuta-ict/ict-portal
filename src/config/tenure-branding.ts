/**
 * The banner the Tenure page wears when a session has no banner of its own — before
 * coronation, or when a coronation was recorded without one.
 *
 * To use your own: put the image in `public/images/` and point this at it (for example
 * "/images/tenure-banner-default.jpg"). A wide image works best — about 1600×600 —
 * with the important part in the middle, because the banner is cropped to fit a phone.
 */
export const DEFAULT_TENURE_BANNER = "/images/tenure-banner-default.svg";
